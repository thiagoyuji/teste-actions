name: "Teste Execucao em matriz"

on:
  issues:
    type: [opened]

jobs: 

  message-starting-process:
    runs-on: ubuntu-latest
    steps:
      - name: Message
        run: echo "Starting process ..."
  
